
const nombre = "Ziortza"
const apellido = "Rey"
const estudiante = nombre + " " + apellido
const estudianteMayus = estudiante.toUpperCase()
const estudianteMinus = estudiante.toLowerCase()
const numconstras = estudiante.length
const primeraconstra = nombre[0]
const ultimaconstra = apellido[apellido.length - 1]
const sinEspacios = estudiante.replace(/ /g, "")
const nomEnEstudiante = estudiante.includes(nombre)
